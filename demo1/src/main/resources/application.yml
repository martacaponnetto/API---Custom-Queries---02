spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db
    username: root
    password: Infinito12-
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create
  jackson:
    serialization:
      fail-on-empty-beans: false